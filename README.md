ios-mapbox-interactive-map
==========================

A starter Xcode project that uses the MapBox SDK to use an interactive enabled offline mbtiles store. This example is based off of an example provided by MapBox. It also makes use of the MapBox geography-class tilestore.
